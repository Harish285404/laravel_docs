               $result = CancelBookingQues::get();$reviewCounts = [    'reasons' => $result->map(function ($review) {        return [            'cancellationReasonText' => $review->reason_text,            'cancellationReasonCode' => $review->reason_code,        ];    })->toArray()];dd($reviewCounts);        $result = $result1->map(function ($review) {    return [        'cancellationReasonText' => $review->reason_text, // Ensure rating is float        'cancellationReasonCode' => $review->reason_code,    // Ensure count is integer    ];})->toArray();